# Despliegue

Destino previsto: Vercel.

## Requisitos

1. Configurar variables de `.env.example` por entorno.
2. Ejecutar `npm run typecheck`, `npm run lint`, `npm run test` y `npm run build`.
3. Aplicar migraciones revisadas en Supabase.
4. Registrar URLs de OAuth y webhooks del dominio definitivo.
5. Verificar RLS y aislamiento entre dos workspaces de prueba.

No se realizó despliegue productivo ni se modificaron dominios.
